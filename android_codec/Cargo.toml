[package]
name = "android_codec"
version = "0.1.0"
authors = ["DroidVM contributors"]
edition = "2021"

[dependencies]
base = { path = "../base" }
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"

[[bin]]
name = "codec_probe"
path = "src/probe.rs"
